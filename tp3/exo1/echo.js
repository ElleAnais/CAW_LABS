// echo.js

// Définir la fonction exf
const exf = function (s, n) {
    for (let i = 0; i < n; i++) {
      console.log(s);
    }
  };
  
  // Appels à la fonction exf
  exf("echo", 5);
  exf("JS côté serveur", 10);
  